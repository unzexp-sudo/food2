fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.7
pydantic-settings>=2.3
PyJWT>=2.8
httpx>=0.27
python-multipart>=0.0.9
openpyxl>=3.1
pypdf>=4.2
# Production database driver. docker-compose.yml uses PostgreSQL, so the
# deployed image MUST have the driver baked in — otherwise SQLAlchemy can't
# resolve the `postgresql+psycopg2` dialect at import time and uvicorn
# exits before binding to PORT, which makes Railway report
# "Application failed to respond". `psycopg2-binary` ships its own libpq so
# it works on `python:3.13-slim` without any extra apt packages.
psycopg2-binary>=2.9
